#algos 
#algos/04stack 

# [739. Daily Temperatures](https://leetcode.com/problems/daily-temperatures/)
?
## Сложность
* Time: O(n)
* Space: O(n)
## Визуально
```
Monotonic decreasing stack

temps = [73,74,75,71,69,72,76,73]  Дан список температур temps на каждый день, 
res   = [1,1,4,2,1,1,0,0]          начиная с нулевого (индексы). 
                                   Нужно вернуть массив res длины temps,
								   в котором отражено сколько дней нужно ждать
								   c i-той позиции чтобы температура стала 
								   больше(стало теплее):
								   0 день: 73C|> нужно подождать 1 день с 0го
								   1 день: 74С|> чтобы стало теплее
								   Если такого дня нет, оставляем 0.

res = [0,0,0,0,0,0,0,0]            Задача решается с использованием монотонного
stack = [(i,t)]                    стека: каждое след. значение меньше или равно
                                   предыдущего.
								   Сначала заполняем res нулями, на каждый день.

[0,0,0,0,0,0,0,0]				   На каждый итерации, кладем в стек тапл из двух
[[0, 73]]						   элементов: [индекс в temps, значение в temps]
                                   
[0,0,0,0,0,0,0,0]				   Но если стек не пустой, то нужно проверить
[[0, 73], [1,74]]				   не лежит ли в нем температура, меньшая
                                   текущей. Если да, и текущая T > T на стеке,
								   то мы нашли искомый день.
[1,0,0,0,0,0,0,0]	               Забираем тапл из стека. В тапле 2 значения:
[] , (74>73, 1-0 = 1)              индекс дня(idx) для которого ищем, и его T.
                                   Чтобы посчитать сколько дней прошло, нужно
								   от текущего индекса отнять idx
								   и(!!!) записать результат в res по idx.

[[75,2],[71,3],[69,4]]			   Если T на верхушке стека меньше, чем текущая				                       то просто складываем в стек, пока не найдем.
```
## Код
### Правильный (O(n))
```python
class Solution:
    def dailyTemperatures(self, temperatures: List[int]) -> List[int]:
        t = temperatures
        res = [0]*len(t)
        stack = []
        
        for i,t in enumerate(t):
            while stack and stack[-1][1] < t:
                idx, temp = stack.pop()
                res[idx] = i - idx
            stack.append([i,t])
        return res
```
### Наивный (O(n^2))
```python
class Solution:
    def dailyTemperatures(self, t: List[int]) -> List[int]:
        res = []
        for l in range(len(t)):
            c = 0
            for r in range(l, len(t)+1):
                if r == len(t):
                    res.append(0)
                    break
                elif t[r]-t[l]>=1:
                    res.append(c)
                    break
                c+=1
        
        return res
```